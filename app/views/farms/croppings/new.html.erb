<%= simple_form_for [@farm, @cropping], url: :farm_croppings do |f| %>
  <%= f.error_notification %>
  <%= f.input :crop_id, collection: Crop.all.map{ |a| [a.name, a.id] }, as: :radio_buttons %>
  <%= f.submit "Save Crop" %>
<% end %>
